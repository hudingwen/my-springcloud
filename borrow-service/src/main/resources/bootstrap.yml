server:
  port: 8301
spring:
  application:
    name: borrowservice
  cloud:
    config:
      # 名称，其实就是文件名称
      name: application
      # 配置服务器的地址
      uri: http://localhost:8701
      # 环境
      profile: pro
      # 分支
      label: main